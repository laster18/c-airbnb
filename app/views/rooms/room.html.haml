.host-header
  .host-header-body.clearfix
    .host-header-body__logo.clearfix
      = link_to "#" do
        %img{src: "http://airbnb-info.com/wp/wp-content/uploads/2014/08/image5.jpg", style: "height: 54px;", class: "host-header-body__image"}
    %span.host-header-body__step.clearfix
      ステップ1: 最初は基本情報から

.rooms-new-main
  .rooms-form
    .rooms-form-body
      .rooms-form-body__title
        さあ、#{current_user.first_name}さん!
        %br
        お部屋の掲載準備をはじめましょう。
      .rooms-form-body__head
        .rooms-form-body__sub
          ステップ1
        .rooms-form-body__question
          掲載するのはどんな部屋ですか？

      = form_for @rooms, url: { controllers: :rooms, action: :bedrooms } do |f|
        .rooms-form-body__category
          = f.label :まずカテゴリを絞りましょう
          = f.collection_select :room_category_id, RoomCategory.all, :id, :name
        .rooms-form-body__building-type
          = f.label :次に建物タイプを選んでください
          = f.collection_select :room_building_type_id, RoomBuildingType.all, :id, :name
        .rooms-form-body__room-type
          = f.label :部屋タイプは？
          = f.select :room_type, Room.room_types.keys.to_a, {}
        .rooms-form-body__guest-only
          = f.label :こちらはゲスト専用の部屋ですか？
          %label
            = f.radio_button :only_for_guest, "true"
            はい、主にゲスト専用のお部屋です
          %label
            = f.radio_button :only_for_guest, "false"
            いいえ、こちらに私物も置いています

        .pagenate
          .pagenate__next-button
            = f.button "次へ", type: "submit"
